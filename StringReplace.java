import java.util.Arrays;
import java.util.List;

public class StringReplace {

    public String hideMessage(String message) {
        List<String> notToChange = Arrays.asList("the", "of", "is", "to", "a", "an", "in", "it", "and");
        String[] count = message.split(" ");
        StringBuilder result = new StringBuilder();
        return "hi";
    }
    public static void main(String Args[]){
        String input = "The purpose of this exercise is to test logical thinking.";
    }
}
